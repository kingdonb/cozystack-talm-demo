apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  annotations:
    meta.helm.sh/release-name: kubernetes-mob
    meta.helm.sh/release-namespace: tenant-test-tenant
  labels:
    app.kubernetes.io/managed-by: Helm
    coztstack.io/target-cluster-name: kubernetes-mob
    cozystack.io/repository: system
    helm.toolkit.fluxcd.io/name: kubernetes-mob
    helm.toolkit.fluxcd.io/namespace: tenant-test-tenant
  name: kubernetes-mob-cilium
  namespace: tenant-test-tenant
spec:
  chart:
    spec:
      chart: cozy-cilium
      reconcileStrategy: Revision
      sourceRef:
        kind: HelmRepository
        name: cozystack-system
        namespace: cozy-system
      version: '*'
  dependsOn:
  - name: kubernetes-mob
    namespace: tenant-test-tenant
  install:
    createNamespace: true
  interval: 1m
  kubeConfig:
    secretRef:
      name: kubernetes-mob-kubeconfig
  releaseName: cilium
  storageNamespace: cozy-cilium
  targetNamespace: cozy-cilium
  values:
    cilium:
      autoDirectNodeRoutes: false
      bpf:
        masquerade: true
      cgroup:
        autoMount:
          enabled: true
        hostRoot: /run/cilium/cgroupv2
      cni:
        chainingMode: null
        configMap: ""
        customConf: false
      enableIPv4Masquerade: true
      ipv4NativeRoutingCIDR: ""
      k8sServiceHost: kubernetes-mob.tenant-test-tenant.svc
      k8sServicePort: 6443
      routingMode: tunnel
      tunnel: disabled
